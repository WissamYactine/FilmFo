import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  heading: {
    color: 'white',
    marginLeft: '10px',
    textDecoration: 'none',
    marginBottom: '40px',
  },
  mainContainer: {
    display: 'flex',
    alignItems: 'center',
  },
}));